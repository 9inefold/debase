# Debase

An llvmir utility to remove calls to base class destructors.

- `DEBASE_RUNTIME_ONLY`: Get the runtime library, not the drivers.
- `DEBASE_GENERATE_TARGET_FUNCTION`: Generate `debase_sources`
